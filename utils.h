#ifndef UTILS_H
#define UTILS_H

// Inicializa o gerador de numeros aleatorios, só deve ser chamado apenas no inicio da execucao do programa
void initRandom();

//Devolve um valor inteiro aleatorio entre [a, b]
int intUniformRnd(int a, int b);

#endif /* UTILS_H */
